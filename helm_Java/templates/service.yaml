apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm_Java.fullname" . }}
  labels:
    {{- include "helm_Java.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm_Java.selectorLabels" . | nindent 4 }}
